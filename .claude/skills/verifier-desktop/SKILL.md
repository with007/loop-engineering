---
name: verifier-desktop
description: >
  Desktop GUI verification. Builds the Rust desktop app, launches it, tests
  tray menu interaction, settings window rendering, and clean shutdown.
---

# verifier-desktop

验证 Desktop GUI 变更。

## 启动检查

```bash
<填写编译命令>
```
exit code 0 → 继续。

```bash
<填写清理旧进程命令>
```

```bash
<填写就绪检查命令>
```
不通 → **BLOCKED**。

## 使用方法

1. 读 diff — 哪些文件变了（事件循环？托盘菜单？设置窗口？）
2. 匹配合适的验证原语
3. 按顺序执行：先编译 → 再自动化测试 → 最后探测
4. 没有匹配的 → 跑**默认**流程

```bash
<填写默认验证命令>
```

## 可用工具

- `cargo` — Rust 编译
- `taskkill` — Windows 进程管理
- <填写其他工具>

## 验证原语

> **日志路径**：`<填写开发构建日志路径>`，安装版在 `%LOCALAPPDATA%/<填写应用名>/<填写日志文件名>`。

### 编译检查

```bash
<填写编译命令>
```
确认 exit code 0，无新增 warning。

适用：所有源码文件变更

### 自动化测试

```bash
<填写自动化测试命令>
```
确认 output 含 `<填写通过标记>`。

适用：交互逻辑变更
注意：若项目无自动化测试 → 跳过此项

### 内置测试模式

```bash
<填写启动命令> <填写测试模式参数> &
sleep <填写等待秒数>
cat <填写日志路径>
<填写清理进程命令>
```
确认 log 含 `<填写通过标记>` + `<填写关键日志行>`。

适用：渲染变更、系统初始化变更
注意：若项目无内置测试模式 → 跳过此项

### <填写项目特有的验证原语>

<填写原语描述>

```bash
<填写验证命令>
```

适用：<填写适用条件>

## 探测

<填写：根据应用类型选择探测项>

**托盘类应用**：
- 关闭窗口 → 不应退出（只隐藏，托盘图标仍在）
- 右键托盘 → 菜单正确弹出，标签正确
- 托盘退出 → 进程干净终止
- 重复打开/关闭窗口 → 无空白窗口

**普通窗口应用**：
- 关闭窗口 → 进程终止（或符合预期）
- <填写项目特有的交互探测>

## 清理

```bash
<填写清理命令>
```

## 自更新

- 二进制名变了 → 更新上方命令
- 新增托盘菜单项 → 补充到探测
- 构建系统换了 → 更新编译命令
- <填写其他自更新规则>
