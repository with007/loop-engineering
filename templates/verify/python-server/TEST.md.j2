# 人工验证检查清单 — {{ project_name }}

> 项目类型: Python Server
>
> 自动化验证由 `loop-verify` + `verifier-*` skills 处理（服务启动、API 检查、
> 边界探测、模板渲染等）。本文档专注**需要人眼判断**的验证项：
> 页面渲染效果、交互流程、视觉一致性等 loop-verify 无法自动判断的内容。
>
> 本文件是**人工验证清单参考**，不是必须全部执行的清单。
> 合入时根据变更涉及哪些模块，选择相关检查点来验证。
> 重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。

## 关键页面

浏览器打开以下页面，确认：
- 无白屏、无 500 错误
- 关键元素正常渲染（导航、表格、图表等）
- 核心交互流程可走通

<填写各页面的路径、功能说明和操作方式>

## 模块人工验证示例

以下是各模块的人工验证示例。合入时**只执行变更涉及模块**的验证，未涉及的跳过。

> 项目初始化时 `loop-verify-init` 会根据项目实际模块填充以下示例。

### <填写模块名>

**适用场景**: <填写场景描述，如"变更涉及 HTML 模板、前端 JS、页面路由时执行">

**步骤**:
1. `<填写验证步骤>`
2. `<填写验证步骤>`

**预期**: <填写预期结果>

> 如果变更涉及以上未列出的新模块，参照上述方法推导合适的验证步骤。
> 验证通过后建议将新模块的验证示例添加到本文档。
