# 验证指南 — {{ project_name }}

> 项目类型: {{ preset_display_name }}
>
> 本文件定义项目特定的自动验证步骤。verifier 子代理按此顺序执行。
> 你可以编辑步骤中的具体命令。重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。
>
> 通用验证（diff 审查、变更范围确认等）由 task-runner 框架统一处理，
> 不需要在这里重复。

## 自动化验证流水线

### 1. 运行测试

执行项目的测试命令。Python 项目常见方式：

| 测试框架 | 命令 |
|----------|------|
| pytest | `python -m pytest` |
| unittest | `python -m unittest discover` |
| tox | `tox` |

如果项目没有配置测试（没有 test_*.py 文件或 pytest 配置），
verifier 会自动跳过这一步。没配置时 verifier 会先检测项目中
实际存在的测试框架，再选择对应的命令。

- **命令**: python -m pytest || echo skipped
- **通过条件**: exit code 0

### 2. 代码逻辑检查

检查本次变更涉及的核心逻辑是否正确：
- 条件分支覆盖边界情况
- 函数入参/出参与调用方匹配
- 异常路径有处理

### 3. 模板检查（涉及 HTML/Jinja2 时）

如果变更涉及模板文件（.html / .j2）：
- 用真实数据渲染模板，检查生成内容的关键字段
- 确认模板中引用的 JS 函数、CSS 类、变量在代码库中存在

### 4. 运行时验证

启动服务，验证真实运行状态。

**Web 服务**:
- 后台启动服务，等待端口就绪
- 用 curl 请求关键 API 端点，检查响应状态码和返回结构
- 用 WebFetch 加载主要页面，确认关键内容存在
- 停止服务

**CLI 工具**:
- 运行 `--help` 或子命令，验证基本功能正常

**纯库**: 跳过

- **命令**: 自动检测（verifier 根据项目结构选择验证方式）
- **通过条件**: 服务正常启动并响应
- **非 Web/CLI 项目时**: 跳过
