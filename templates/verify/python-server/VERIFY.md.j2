# 验证指南 — {{ project_name }}

> 项目类型: Python Server
>
> 本文件是 Python Server 类型的默认模板。`loop-test-init` 会根据项目
> 实际情况定制所有内容：增删章节、调整验证手段、替换占位符。
>
> 本文件是**验证方法论参考**，不是必须全部执行的清单。
> verifier 子代理按此执行。
> 验证范围由变更驱动 — 只执行涉及模块的步骤，未涉及的跳过。
> 重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。
>
> 通用验证（diff 审查、变更范围确认等）由 task-runner 框架统一处理。

## 核心原则

**把代码跑起来，亲眼看到结果。** 构建、启动、操作、观察。读代码是为了理解变更，但不是验证——必须让代码真正执行。

**走真实表面，不 import 内部函数。** 用户怎么触发功能，你就怎么触发。用户点按钮就点按钮，用户调 API 就 curl，用户跑命令就跑命令。`from src.xxx import foo` 再 `print(foo(x))` 测的是函数签名，不是系统行为。

**正常路径和边界路径都要覆盖。** 确认变更做到了该做的事，也确认传错参数不崩溃、缺依赖有清晰报错。

## 表面识别

拿到 diff 后，对照下面的**项目表面映射表**找出变更命中了哪些表面。内部函数不是表面——顺藤摸瓜，追到它最终对外暴露的那一层。

> 映射表由 `loop-test-init` 根据项目实际结构生成。项目结构调整后重新运行 `loop setup` 即可更新。

<填写表面映射表>

**diff 是唯一真相。任何口头描述都只是对 diff 的声明。** 两边都对一遍，不一致就是发现。

## 可用工具

| 工具 | 用途 |
|------|------|
| curl | HTTP 请求，检查 API 状态码和返回结构 |
| WebFetch | 加载页面，确认关键元素存在 |
| Python -c | 内联脚本，处理 API 返回数据 |
| Jinja2 渲染 | 用真实数据渲染模板，验证输出内容 |
| Bash 命令 | CLI 验证（--help）、进程管理（启动/停止服务） |
| cargo | Rust 编译（Desktop 托盘程序） |

## 启动应用

> 启动命令由 `loop-test-init` 根据项目配置生成。启动方式有变化时重新运行 `loop setup` 更新。

### Web 服务

```bash
<填写 Web 服务启动命令>
```

### CLI

```bash
<填写 CLI 入口命令>
```

### Desktop 应用

```bash
<填写 Desktop 构建/启动命令>
```

> 上面命令跑不通？参考 README / package.json / Makefile。
> 15 分钟还搞不定 → BLOCKED，说清楚卡在哪里。

## 触发变更

怎么让改动的代码真正执行起来：

- 改了 HTML 模板 → 启动服务，curl 对应的 fragment 端点，确认模板正确渲染
- 改了 API handler → curl 那个接口，看状态码和返回内容
- 改了 CLI 参数 → 带上参数跑
- 改了错误处理 → 故意触发那个错误
- 改了内部函数 → 找到最终触发它的 CLI 命令 / API / 页面，运行那个

**动手前先看一遍计划。** 如果步骤全是"构建 → 跑测试"，你写的是 CI 脚本，不是验证计划。确保至少有一个步骤碰到了真实表面。

**走完整路径，不走捷径。** 用户点按钮，你就点按钮（Playwright）；用户调接口，你就调接口。GUI 应用不要拿消息模拟代替真实交互——CLAUDE.md 规则 2 同样适用。

## 边界探测

验证不止是确认正常路径能走通。对每个涉及的表面，至少试一项偏离正常用法的操作。没试出问题也是一步："🔍 传空 → 报错清晰"。

**API 接口：**
- 用错误的 HTTP method → 应返回 405
- 不传必填参数 → 应返回 422，错误信息能看懂
- 传不存在的资源 ID → 应返回 404
- 传超长输入 / 特殊字符 → 不能崩溃

**Web 页面（fragment 端点）：**
- 不传 project 参数 → 应有降级处理或清晰报错
- 带 `HX-Request: true` header → fragment 正确渲染（非全页）
- 浏览器交互 → 用 Playwright 模拟真实操作路径

**CLI 命令：**
- 传未定义的 flag → 应报错并列出可用选项
- 不传必填位置参数 → 应报错，不能静默执行
- 传不存在的文件路径 → 应报清晰错误

**Desktop GUI：**
- PostMessage 模拟右键托盘 → 菜单正确弹出
- 关闭设置窗口 → 应用不退出（只隐藏）
- 托盘退出 → 进程正常终止

**状态 / 持久化：**
- 同一操作做两遍 → 结果一致
- 反复启动/停止 → 不残留僵尸进程
- 两个终端同时操作 → 不冲突

> 上面不是检查清单——挑变更可能影响到的去测。覆盖完明显的相邻区域就停。

## 破坏性操作

变更涉及删除、发布、外部写入，又没有 dry-run 或安全目标时：
- 验证周围能验证的部分
- 说明跳过了哪个路径，为什么
- 不要直接触发破坏性操作

## 报告格式

验证完成后按以下格式输出。这个报告会和 implementer 的输出一起写入 git commit message，人审查时扫一眼就能理解变更全貌：实现思路 → 变更概要 → 验证方案 → 结论 → 已知风险。

```
## 验证方案
<验证思路概述：从哪些角度验证、覆盖了哪些表面>
- 测试点 1: <验证方法>
- 测试点 2: <验证方法>

## 验证过程
1. ✅ <正常路径操作> → <观察到的结果>
   <证据：终端输出、响应体>
2. 🔍 <探测操作> → <观察到的结果>
   <证据>

## 结论
**PASS** — <原因>

## 已知局限
- <未覆盖的场景>
- <已知边界条件>

## 发现
- ⚠️ <任何值得注意的事项>
```

**标记说明：**
- `✅` — 正常路径验证，预期结果符合
- `🔍` — 探测步骤，偏离正常用法、试图触发异常的尝试。至少一个
- `❌` — 失败步骤
- `⚠️` — 跳过（环境限制等合理原因）

**结论说明：**
- **PASS** — 应用跑起来了，变更做到了该做的事，探测未发现异常
- **FAIL** — 没做到声明的事、破坏了其他功能、或探测发现严重问题
- **BLOCKED** — 到不了能观察变更的状态。构建失败、缺依赖、启动不了。这不是对变更本身的评判，卡在哪一步说清楚

**拿不准就选 FAIL。** 假 PASS 会放行有问题的代码；假 FAIL 顶多多看一遍。

## 模板检查

变更涉及 `.html` / `.j2` 文件时执行。用 Jinja2 `Environment` + `FileSystemLoader` 渲染改动过的模板，Python assert 检查渲染结果包含关键字段。

HTML 模板变更时额外检查：
- 模板中引用的 JS 函数、CSS 类、Alpine.js 指令在代码库中有对应实现
- `x-model` / `@click` 等指令依赖的 store/函数存在

## 模块验证示例

各模块的验证示例。verifier **只运行变更涉及模块**的步骤。

> 项目初始化时 `loop-test-init` 根据实际模块填充。

### <填写模块名>

**触发条件**: <填写触发条件>

**表面**: <填写：Web页面 / API / CLI / 模板渲染 / Desktop GUI>

**验证步骤**:
1. `<填写验证步骤>`

**探测**:
- `<填写至少一项偏离正常路径的探测>`

> 变更涉及未列出的新模块时，参照上面的"表面识别"和"边界探测"推导验证步骤。
> 验证通过后建议把新模块的示例补充到这里。
