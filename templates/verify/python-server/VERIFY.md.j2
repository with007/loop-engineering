# 验证指南 — {{ project_name }}

> 本文件是 Python Server 类型的默认模板。`loop-test-init` 会根据项目
> 实际情况定制所有内容：增删模块示例、调整验证手段、替换占位符。
> 重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。
>
> 通用验证（diff 审查、变更范围确认等）由 task-runner 框架统一处理。

## 测试工具

| 工具 | 用途 |
|------|------|
| curl | HTTP 请求，检查 API 状态码和返回结构 |
| WebFetch | 加载页面，确认关键元素存在 |
| Python -c | 内联脚本，assert 检查返回值 |
| Jinja2 渲染 | 用真实数据渲染模板，验证输出内容 |
| <填写测试框架> | <填写测试框架用途> |
| Bash 命令 | CLI 验证（--help）、进程管理（启动/停止服务） |

## 自动化验证手段

### 1. 运行测试

```bash
<填写测试命令>
```
- **通过条件**: <填写通过条件，如 exit code 0>

### 2. 代码逻辑检查

读变更文件的 diff，检查：
- 条件分支覆盖边界情况
- 函数入参/出参与调用方匹配
- 异常路径有处理
- 导入的模块存在且版本兼容

变更涉及的模块见下方"模块验证示例"。

### 3. 模板检查

变更涉及 `.html` / `.j2` 文件时执行。用 jinja2 `Environment` + `FileSystemLoader` 渲染变更的模板，Python assert 检查渲染结果包含关键字段。

HTML 模板变更时额外检查：
- 模板中引用的 JS 函数、CSS 类、Alpine.js 指令在代码库中有对应实现
- `x-model` / `@click` 等指令对应的 store/函数存在

### 4. 运行时验证

#### 4a. 安装依赖

```bash
<填写安装命令>
```
exit code 0 = 通过。

#### 4b. CLI 验证

```bash
<填写 CLI 入口命令> --help
<填写核心子命令验证>
```

#### 4c. Web 服务

```bash
# 后台启动服务
<填写启动命令> &
SERVER_PID=$!
sleep 3
```

API 端点（curl 检查）：
```bash
curl -s -o /dev/null -w "%{http_code}" <填写关键 API URL>
# 期望: 200
```

Web 页面（WebFetch 检查关键元素）：
- `<填写页面 URL>` — 确认含 `<填写关键元素>`

停止服务：
```bash
kill $SERVER_PID 2>/dev/null
```

## 模块验证示例

以下是各模块的自动化验证示例。verifier **只运行变更涉及模块**的步骤。

> 项目初始化时 `loop-test-init` 会根据项目实际模块填充以下示例。
> 下面是一个 Web 页面的通用示例模板：

### <填写模块名>

**触发条件**: <填写触发条件，如"变更涉及 HTML 模板、前端 JS、页面路由">

**验证步骤**:
1. `<填写验证步骤>`
2. `<填写验证步骤>`
3. 停止服务

> 如果变更涉及以上未列出的新模块，参照"自动化验证手段"推导合适的验证步骤。
> 验证通过后建议将新模块的验证示例添加到本文档。
