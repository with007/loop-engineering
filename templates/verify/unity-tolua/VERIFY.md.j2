# 验证指南 — {{ project_name }}

> 项目类型: {{ preset_display_name }}
>
> 本文件定义 verifier 子代理的自动验证流程。你可以编辑步骤中的具体命令。
> 重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。

## 自动化验证流水线

验证子代理按以下顺序执行。非可选步骤失败则停止。

### 1. 编译检查

触发 Unity 编译并检查 Console 输出。如果项目中的 C# 代码有编译错误，
这一步会捕获。

- **工具**: MCP `refresh_unity(wait_for_ready=true)` → `read_console`
- **超时**: 120s
- **通过条件**: Console 无 error

### 2. Lua 路径生成

当 diff 中包含新增的 `.lua` 文件时执行。例如 PVPProject7 使用
`scripts/genLuaPath.py` 扫描 `Assets/FW/Lua` 和 `Assets/Battle/Lua`
目录，生成 `LuaPath.lua` 注册文件。如果你的项目使用类似的机制，
替换为对应的脚本路径。

- **条件**: diff 中有新增 `.lua` 文件
- **命令**: python scripts/genLuaPath.py
- **通过条件**: exit code 0

### 3. Lua 运行时测试

在 Play Mode 中执行注册的 Lua 自动测试。测试用例通过
`[AUTO TEST: 名称] PASS/FAIL` 标记输出结果到 Console。

如果你的项目使用类似 PVPProject7 的 DebugConsole 注册模式：
- 在 `config.json` 中设置 `autoTestBattle: true`
- 测试用例通过 `register_lua_test()` 注册到 DebugConsole
- verifier 通过 `runtime-test` skill 执行注册的测试

- **工具**: `runtime-test` skill
- **超时**: 30s
- **通过条件**: 所有 `[AUTO TEST: xx]` 标记 PASS，无 FAIL

### 4. 代码逻辑检查

检查本次变更涉及的核心逻辑是否正确：
- 条件分支覆盖边界情况
- 函数入参/出参与调用方匹配
- 异常路径有处理
