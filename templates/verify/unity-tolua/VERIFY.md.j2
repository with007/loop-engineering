# 验证指南 — {{ project_name }}

> 项目类型: Unity + ToLua
>
> 本文件是 Unity + ToLua 类型的默认模板。`loop-test-init` 会根据项目
> 实际情况定制所有内容：增删章节、调整验证手段、替换占位符。
>
> 本文件是**验证方法论参考**，不是必须全部执行的清单。
> verifier 子代理按此执行。
> 验证范围由变更驱动 — 只执行涉及模块的步骤，未涉及的跳过。
> 重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。
>
> 通用验证（diff 审查、变更范围确认等）由 task-runner 框架统一处理。

## 测试工具

| 工具 | 用途 |
|------|------|
| MCP refresh_unity | 触发 Unity 编译，等待完成 |
| MCP read_console | 读取 Console 输出，检查编译 error |
| MCP manage_editor | 控制 Play Mode 进入/退出 |
| runtime-test skill | 执行注册的 Lua 自动测试，检查 PASS/FAIL 标记 |
| Bash 命令 | Lua 路径生成脚本、进程管理 |

## 自动化验证手段

### 1. 编译检查

- **工具**: MCP `refresh_unity(wait_for_ready=true)` → `read_console`
- **超时**: 120s
- **通过条件**: Console 无 error

### 2. Lua 路径生成

当 diff 中包含新增的 `.lua` 文件时执行。

- **触发条件**: diff 中有新增 `.lua` 文件
- **命令**: <填写 Lua 路径生成脚本>
- **通过条件**: exit code 0

### 3. Lua 运行时测试

在 Play Mode 中执行注册的 Lua 自动测试。

- **工具**: `runtime-test` skill
- **超时**: 30s
- **通过条件**: 所有 `[AUTO TEST: xx]` 标记 PASS，无 FAIL

### 4. 代码逻辑检查

读变更文件的 diff，检查：
- 条件分支覆盖边界情况
- 函数入参/出参与调用方匹配
- 异常路径有处理

## 模块验证示例

以下是各模块的自动化验证示例。verifier **只运行变更涉及模块**的步骤。

> 项目初始化时 `loop-test-init` 会根据项目实际模块填充以下示例。

### C# 脚本

**触发条件**: 变更涉及 `.cs` 文件。

**验证步骤**:
1. MCP `refresh_unity` → `read_console`，确认 0 error
2. 如有新增 `.lua` 文件，运行 Lua 路径生成脚本

### Lua 逻辑

**触发条件**: 变更涉及 `.lua` 文件。

**验证步骤**:
1. MCP `refresh_unity` → `read_console`，确认 0 error
2. 进入 Play Mode，通过 `runtime-test` skill 运行注册的测试
3. 确认 Console 中 `[AUTO TEST: xx]` 全部 PASS

### UI/预制体

**触发条件**: 变更涉及 `.prefab`、UI 相关文件。

**验证步骤**:
1. MCP `refresh_unity` → `read_console`，确认 0 error
2. 进入 Play Mode，检查对应 UI 可正常打开和交互

> 如果变更涉及以上未列出的新模块，参照"自动化验证手段"推导合适的验证步骤。
> 验证通过后建议将新模块的验证示例添加到本文档。
