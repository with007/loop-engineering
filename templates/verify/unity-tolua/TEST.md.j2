# 人工验证检查清单 — {{ project_name }}

> 项目类型: Unity + ToLua
>
> 自动化验证由 `loop-verify` + `verifier-*` skills 处理（编译检查、Lua
> 路径生成、运行时测试等）。本文档专注**需要人眼判断**的验证项：
> 画面表现、帧率、UI 交互、视觉一致性等 loop-verify 无法自动判断的内容。
>
> 本文件是**人工验证清单参考**，不是必须全部执行的清单。
> 合入时根据变更涉及哪些模块，选择相关检查点来验证。
> 重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。

## 运行时验证

1. 进入 Play Mode，走一遍本次变更涉及的核心功能路径
   - 战斗相关变更：进入战斗，确认画面表现正常
   - UI 变更：打开对应面板，确认布局正确、交互正常
   - 系统功能：触发对应路径，检查行为符合预期
2. 关注帧率是否有明显下降

## 模块人工验证示例

以下是各模块的人工验证示例。合入时**只执行变更涉及模块**的验证，未涉及的跳过。

> 项目初始化时 `loop-verify-init` 会根据项目实际模块填充以下示例。

### C# 脚本

**适用场景**: 变更涉及 `.cs` 文件。

**步骤**:
1. 等待编译完成
2. 进入 Play Mode，确认变更的逻辑表现正常
3. <填写后续验证步骤>

**预期**: 无异常，功能符合预期

### Lua 逻辑

**适用场景**: 变更涉及 `.lua` 文件。

**步骤**:
1. 进入 Play Mode
2. 触发变更涉及的功能路径
3. 确认行为符合预期

**预期**: 功能正常，无 Lua 异常

### UI/预制体

**适用场景**: 变更涉及 `.prefab`、UI 相关文件。

**步骤**:
1. 打开对应场景/面板
2. 确认布局、文字、交互正常
3. 检查不同分辨率下的表现

**预期**: UI 正常显示，交互响应正确

> 如果变更涉及以上未列出的新模块，参照上述方法推导合适的验证步骤。
> 验证通过后建议将新模块的验证示例添加到本文档。
