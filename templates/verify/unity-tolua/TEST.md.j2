# 手动测试指南 — {{ project_name }}

> 项目类型: Unity + ToLua
>
> 本文件是 Unity + ToLua 类型的默认模板。`loop-test-init` 会根据项目
> 实际情况定制所有内容：增删章节、调整验证手段、替换占位符。
> 重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。

## 测试工具

| 工具 | 用途 |
|------|------|
| Unity Editor | 编译项目、Play Mode、场景预览 |
| Unity Console | 查看编译 error/warning、运行时日志 |
| 终端 | Lua 路径生成、进程管理 |

## 测试手段

### 环境准备

测试在 agent worktree 中执行。启动前确认：

1. Unity Editor 已打开对应 worktree 的项目
2. 项目运行时配置就绪
3. <填写其他环境准备步骤>

### 编译检查

通过 MCP `refresh_unity` 触发编译，`read_console` 确认无 error。

### 启动项目

<填写进入 Play Mode 的方式>

### 运行时验证

1. 走一遍本次变更涉及的核心功能路径
   - 战斗相关变更：进入战斗，确认画面表现正常
   - UI 变更：打开对应面板，确认布局正确、交互正常
   - 系统功能：触发对应路径，检查行为符合预期
2. 关注帧率是否有明显下降

### Lua 自动测试

通过 `runtime-test` 技能运行注册的 Lua 测试，检查 `[AUTO TEST: xx] PASS/FAIL` 标记。

## 测试完成后

1. 退出 Play Mode
2. 确认 Console 无残留 error 和 warning
3. <填写配置恢复步骤>

## 模块测试示例

以下是各模块的测试示例。合入时**只执行变更涉及模块**的测试，未涉及的跳过。

> 项目初始化时 `loop-test-init` 会根据项目实际模块填充以下示例。

### C# 脚本

**适用场景**: 变更涉及 `.cs` 文件。

**步骤**:
1. 等待编译完成
2. 检查 Console 窗口：0 errors
3. <填写后续验证步骤>

**预期**: 编译通过，无新增 error

### Lua 逻辑

**适用场景**: 变更涉及 `.lua` 文件。

**步骤**:
1. 进入 Play Mode
2. 触发变更涉及的功能路径
3. 检查 Console 无 Lua 异常

**预期**: 功能正常，无 Lua 异常

### UI/预制体

**适用场景**: 变更涉及 `.prefab`、UI 相关文件。

**步骤**:
1. 打开对应场景/面板
2. 确认布局、文字、交互正常
3. 检查不同分辨率下的表现

**预期**: UI 正常显示，交互响应正确

> 如果变更涉及以上未列出的新模块，参照"测试手段"中的方法推导合适的测试步骤。
> 测试通过后建议将新模块的测试示例添加到本文档。
