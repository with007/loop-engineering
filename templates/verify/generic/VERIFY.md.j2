# 验证指南 — {{ project_name }}

> 项目类型: {{ preset_display_name }} | 生成自 `templates/verify/generic/VERIFY.md.j2`
>
> 你可以编辑此文件来添加项目特定的验证步骤。重新运行 `loop setup` 会覆盖 — 请备份自定义内容。

## 自动化验证流水线

验证子代理按以下步骤执行。

### 1. 构建检查

- **命令**: `npm run build || echo skipped`
- **通过条件**: exit code 0（或跳过）

### 2. 运行测试

- **命令**: `npm test || echo skipped`
- **通过条件**: exit code 0（或跳过）

### 3. 代码审查

- 读完整 diff 和 implementer 报告，理解变更全貌
- 确认变更范围正确、无多余文件
- 检查代码逻辑正确性

## 验证输出格式

```
## 验证方案
- 测试点 1: <验证方法>
- 测试点 2: <验证方法>

## 验证结果
<所有测试点逐一结果>

**最终**: PASS 或 FAIL + 原因

## 已知局限
- <未覆盖的场景>
- <已知边界条件>
```
