# 验证指南 — {{ project_name }}

> 项目类型: {{ preset_display_name }}
>
> 本文件定义项目特定的自动化验证步骤。verifier 子代理按此执行。
> 验证范围由变更驱动 — 只执行涉及模块的步骤，未涉及的跳过。
> 你可以编辑步骤中的具体命令。重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。
>
> 通用验证（diff 审查、变更范围确认等）由 task-runner 框架统一处理。

## 测试工具

| 工具 | 用途 |
|------|------|
| curl | HTTP 请求，检查 API 状态码和返回结构 |
| WebFetch | 加载页面，确认关键元素存在 |
| Bash 命令 | 构建/安装依赖、CLI 验证、进程管理 |
| 项目构建工具 | npm/node、pip、go、cargo 等，按项目类型自动检测 |

## 自动化验证手段

以下是 verifier 可用的自动化验证方法。

### 1. 构建检查

运行项目的构建命令。verifier 自动检测项目类型选择命令：

| 项目类型 | 构建命令 |
|----------|----------|
| Node | `npm run build` |
| Python | `pip install -e .` |
| Go | `go build ./...` |
| Rust | `cargo build` |

- **通过条件**: exit code 0（无构建步骤则跳过）

### 2. 运行测试

执行项目的测试命令。verifier 自动检测项目类型选择命令：

| 项目类型 | 测试命令 |
|----------|----------|
| Node | `npm test` |
| Python | `python -m pytest` |
| Go | `go test ./...` |
| Rust | `cargo test` |

- **通过条件**: exit code 0（无测试则跳过）

### 3. 代码逻辑检查

读变更文件的 diff，检查：
- 条件分支覆盖边界情况
- 函数入参/出参与调用方匹配
- 异常路径有处理
- 导入的模块存在且版本兼容

### 4. 运行时验证

如果项目有可执行入口（Web 服务、CLI 工具），启动并验证。

**Web 服务**:
```bash
# 后台启动
{{ test_serve }} &
sleep 3
```
- curl 关键 API 端点，检查状态码和返回结构
- WebFetch 关键页面，确认关键元素存在
- 停止服务

**CLI 工具**:
```bash
{{ test_serve }} --help    # exit code 0
```

**纯库**: 跳过运行时验证。

## 模块验证示例

以下是各模块的自动化验证示例。verifier **只运行变更涉及模块**的步骤。

### Web 页面

**触发条件**: 变更涉及 HTML 模板、前端文件、页面路由。

**验证步骤**:
1. 构建/安装依赖
2. 启动服务，curl 关键 API 端点确认 200
3. WebFetch 关键页面确认元素存在
4. 停止服务

### CLI 命令

**触发条件**: 变更涉及 CLI 入口、参数解析、子命令。

**验证步骤**:
1. 构建/安装依赖
2. `--help` — exit code 0
3. 执行核心子命令验证输出

### API 端点

**触发条件**: 变更涉及 API 路由、请求处理。

**验证步骤**:
1. 构建/安装依赖，启动服务
2. curl 变更涉及的端点，检查状态码和返回结构
3. 停止服务

> 如果变更涉及以上未列出的新模块，参照"自动化验证手段"推导合适的验证步骤。
> 验证通过后建议将新模块的验证示例添加到本文档。
