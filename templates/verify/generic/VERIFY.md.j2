# 验证指南 — {{ project_name }}

> 项目类型: Generic
>
> 本文件是 Generic 类型的默认模板。`loop-test-init` 会根据项目
> 实际情况定制所有内容：增删章节、调整验证手段、替换占位符。
> 重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。
>
> 通用验证（diff 审查、变更范围确认等）由 task-runner 框架统一处理。

## 测试工具

| 工具 | 用途 |
|------|------|
| <填写工具名> | <填写用途> |
| Bash 命令 | 构建/安装依赖、CLI 验证、进程管理 |

## 自动化验证手段

### 1. 构建检查

```bash
<填写构建命令>
```
- **通过条件**: exit code 0

### 2. 运行测试

```bash
<填写测试命令>
```
- **通过条件**: exit code 0

### 3. 代码逻辑检查

读变更文件的 diff，检查：
- 条件分支覆盖边界情况
- 函数入参/出参与调用方匹配
- 异常路径有处理
- 导入的模块存在且版本兼容

### 4. 运行时验证

**Web 服务**:
```bash
<填写启动命令> &
sleep 3
```
- curl 关键 API 端点，检查状态码和返回结构
- WebFetch 关键页面，确认关键元素存在
- 停止服务

**CLI 工具**:
```bash
<填写 CLI 入口命令> --help
```

**纯库**:
跳过运行时验证。

## 模块验证示例

以下是各模块的自动化验证示例。verifier **只运行变更涉及模块**的步骤。

> 项目初始化时 `loop-test-init` 会根据项目实际模块填充以下示例。

### <填写模块名>

**触发条件**: <填写触发条件>

**验证步骤**:
1. `<填写验证步骤>`

> 如果变更涉及以上未列出的新模块，参照"自动化验证手段"推导合适的验证步骤。
> 验证通过后建议将新模块的验证示例添加到本文档。
