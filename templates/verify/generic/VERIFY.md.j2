# 验证指南 — {{ project_name }}

> 项目类型: {{ preset_display_name }}
>
> 本文件定义项目特定的自动验证步骤。verifier 子代理按此顺序执行。
> 你可以编辑步骤中的具体命令。重新运行 `loop setup` 会覆盖 — 有自定义内容请备份。
>
> 通用验证（diff 审查、变更范围确认等）由 task-runner 框架统一处理。

## 自动化验证流水线

### 1. 构建检查

运行项目的构建命令。常见例子：

| 项目类型 | 构建命令 |
|----------|----------|
| Node | `npm run build` |
| Python | `pip install -e .` |
| Go | `go build ./...` |
| Rust | `cargo build` |

如果项目没有构建步骤（解释型语言、纯脚本等），verifier 会自动跳过。

- **命令**: 自动检测
- **通过条件**: exit code 0（无构建步骤则跳过）

### 2. 运行测试

执行项目的测试命令。常见例子：

| 项目类型 | 测试命令 |
|----------|----------|
| Node | `npm test` |
| Python | `python -m pytest` |
| Go | `go test ./...` |
| Rust | `cargo test` |

如果项目没有测试，verifier 会自动跳过。

- **命令**: 自动检测
- **通过条件**: exit code 0（无测试则跳过）

### 3. 代码逻辑检查

检查本次变更涉及的核心逻辑是否正确：
- 条件分支覆盖边界情况
- 函数入参/出参与调用方匹配
- 异常路径有处理

### 4. 运行时验证

如果项目有可执行入口（Web 服务、CLI 工具），启动并验证真实运行状态。
纯库项目跳过。

- **命令**: 自动检测
- **通过条件**: 服务正常启动并响应
- **纯库时**: 跳过
