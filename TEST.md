# 人工验证清单 — loop-engineering

> 项目类型: Python Server
>
> 自动化验证（API、CLI、Web 页面 fragment 检查等）由 `loop-verify` + `verifier-*` skills 处理。
> 本文档专注于 loop-verify 无法自动判断的**人工复核项**。

## 关键页面

浏览器打开服务后，逐页检查以下项目：

| 路径 | 功能 | 检查点 |
|------|------|--------|
| `/` | 首页/概览 | 图表正常渲染，无白屏或 500 错误，数据加载完整 |
| `/settings` | 设置页 | 表单元素正常显示，保存按钮可用，交互流程完整 |
| `<填写：其他关键页面路径>` | `<填写：功能说明>` | `<填写：人眼检查项>` |

> 如果项目没有某些页面，删除对应行。如果有关键页面不在列表中，补充。

## 模块人工验证示例

以下是各模块的人工验证示例。合入时**只执行变更涉及模块**的测试，未涉及的跳过。

> 项目初始化时 `loop-verify-init` 会根据项目实际模块填充以下示例。

### Web 页面/模板

**适用场景**: 变更涉及 HTML 模板、前端 JS、CSS 样式时执行。

**步骤**:
1. 浏览器打开变更涉及的页面
2. 确认页面正常渲染（无白屏、无 500）
3. 检查关键元素存在且交互正常（按钮、表单、导航）
4. 确认深色/浅色模式（如支持）下样式正确

**预期**: 页面正常渲染，交互符合预期

### <填写：其他模块名>

**适用场景**: <填写：变更涉及哪些文件时执行>

**步骤**:
1. `<填写：人工测试步骤>`

**预期**: <填写：预期结果>

> 如果变更涉及以上未列出的新模块，参照上述方法推导合适的检查点。
> 测试通过后建议将新模块的检查点添加到本文档。